name: Tests (Deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    if: false
    steps:
      - name: Deprecated notice
        run: |
          echo "This workflow is deprecated. Use CI workflow in .github/workflows/ci.yml."
